<template>
  <div class="prompt_case" v-if="visible">
      <div class="prompt_confirm">
        <div class="prompt_confirm_title">{{title}}</div>
        <div class="prompt_confirm_content">{{content}}</div>
        <slot name="confirm_footer">
          <!-- <div class="prompt_confirm_footer">
            <button>确认</button>
            <button>取消</button>
          </div> -->
        </slot>
      </div>
    </div>
</template>

<script type="text/javascript">
  export default {
    name: 'prompt-confirm',
    props: {
      title: {
        type: String,
        default: 'title'
      },
      content: {
        type: String,
        default: 'conent'
      },
      visible: {
        type: Boolean,
        default: false
      },
    },
    data () {
      return {
      }
    },
    methods: {
      close () {
        this.$emit('update:visible', false)
      }
    }
  }
</script>

<style scope>
.prompt_case {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 999999;
}
.prompt_confirm {
  position: absolute;
  top: 50%;
  left: 50%;
  padding: 10px 20px;
  background: #fff;
  border-radius: 4px;
  transform: translate(-50%, -50%);
  min-width: 300px;
}
.prompt_confirm > .prompt_confirm_title,
.prompt_confirm > .prompt_confirm_content {
  margin: 0;
  padding: 0;
  font-weight: normal;
}
.prompt_confirm > .prompt_confirm_title {
  font-size: 22px;
}
.prompt_confirm > .prompt_confirm_content {
  text-align: center;
  margin: 15px 0;
}
.prompt_confirm_footer {
  content: "\0020";
  display: block;
  height: 0;
  clear: both;
}
.prompt_confirm_footer > button {
  float:right;
  background: #409eff;
  border: none;
  cursor: pointer;
  color: #fff;
  padding: 5px 12px;
  border-radius:4px
}
.prompt_confirm_footer > button:first-child{
  margin-left:15px
}
</style>
